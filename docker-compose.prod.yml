services:
  frontend:
    image: 264212885938.dkr.ecr.ap-south-1.amazonaws.com/kind-connect:latest
    #build:
      #context: .
      #dockerfile: Dockerfile.prod
    container_name: kind-connect-prod

    restart: unless-stopped

    ports:
      - 80:80

  
services:
  frontend:
    build: .
    container_name: kind-connect-compose

    ports:
      - 5173:5173

    environment:
      CHOKIDAR_USEPOLLING: "true"

    volumes:
      - .:/app
      - /app/node_modules

    command: npm run dev -- --host